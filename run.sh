#run to start dicternal
#1. clone the repository dicternal for linux
#2. execute the run.sh to set up 
#3. and you can use it any time
echo "export PATH=$PATH:$HOME/dict/" >> $HOME/.bashrc
source ~/.bashrc
